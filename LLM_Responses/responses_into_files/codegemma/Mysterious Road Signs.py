def find(n):   # n - number of signs in test case     list =[]  for i range (106) : list[i] [False, False]; # false means not visited yet.       ans = []      maxk= 4;        minv=-2**38 ; maxw=(- minV);         # we will use these variables to store the maximum and minimum values of distances in each direction    for i range (n) :     d, a ,b  in map(int,(input().split()));      list[a][False] = True; list [ b ] 
        if d > maxk:            max k=   dk ; minv=-2**38           minw=( -mk);         ans.append([i +1, a ,b]);    # ans will store the number of sign and its distances in each direction     print(f"Case #{n}: {len ( set ) }  { len [set]}")   


for _ range int((input())) : findint())